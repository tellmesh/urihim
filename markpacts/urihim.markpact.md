# UriPack: urihim

Thin Markpact — capabilities from [`urihim/urihim/manifest.yaml`](urihim/urihim/manifest.yaml).
Handlers: existing ``python://`` refs (code in `urihim/`, not duplicated here).

Generated by `urisys/scripts/generate_pack_markpacts.py`.

```yaml markpact:pack
apiVersion: urisys.io/v1
kind: UriPack
metadata:
  id: urihim
  version: '1'
  language: python
description: Human input mock (mouse/keyboard) for urisys-node.
schemes:
- him
capabilities:
- id: him-mouse-status
  uri: him://{host}/mouse/query/status
  kind: query
  operation: him.mouse.status
  handler: python://urihim.handlers:mouse_status
  side_effects: false
  approval: not_required
- id: him-mouse-move
  uri: him://{host}/mouse/command/move
  kind: command
  operation: him.mouse.move
  handler: python://urihim.handlers:mouse_move
  side_effects: true
  approval: required
- id: him-mouse-click
  uri: him://{host}/mouse/command/click
  kind: command
  operation: him.mouse.click
  handler: python://urihim.handlers:mouse_click
  side_effects: true
  approval: required
- id: him-mouse-scroll
  uri: him://{host}/mouse/command/scroll
  kind: command
  operation: him.mouse.scroll
  handler: python://urihim.handlers:mouse_scroll
  side_effects: true
  approval: required
- id: him-keyboard-type
  uri: him://{host}/keyboard/command/type
  kind: command
  operation: him.keyboard.type
  handler: python://urihim.handlers:keyboard_type
  side_effects: true
  approval: required
- id: him-keyboard-type_text
  uri: him://{host}/keyboard/command/type-text
  kind: command
  operation: him.keyboard.type_text
  handler: python://urihim.handlers:keyboard_type
  side_effects: true
  approval: required
- id: him-keyboard-hotkey
  uri: him://{host}/keyboard/command/hotkey
  kind: command
  operation: him.keyboard.hotkey
  handler: python://urihim.handlers:keyboard_hotkey
  side_effects: true
  approval: required
- id: him-keyboard-key
  uri: him://{host}/keyboard/command/key
  kind: command
  operation: him.keyboard.key
  handler: python://urihim.handlers:keyboard_key
  side_effects: true
  approval: required
policy:
  default: deny_mutations_without_approval
runtime:
  default_environment: real
  supports:
  - mock
  - local
  - docker
  expose:
  - pack
  - service
  - flow
  - interface
  - adapter
```

```yaml markpact:run
scheme: him
default: flow
modes:
- pack
- service
- flow
- interface
- adapter
service:
  port_hint: 8789
  path: /uri/call
flow:
  ids:
  - him-smoke
uses: []
adapter:
  call: POST /uri/call
  events: GET /events
```

```yaml markpact:tests
tests:
- id: him.mouse.status_query
  uri: him://local/mouse/query/status
  context:
    approved: true
    dry_run: true
    environment: mock
  expect:
    ok: true
    operation: him.mouse.status
- id: him.mouse.move_command
  uri: him://local/mouse/command/move
  context:
    approved: true
    dry_run: true
    environment: mock
  expect:
    ok: true
    operation: him.mouse.move
  payload: {}
```

```yaml markpact:flow id=him-smoke
flow:
  id: him-smoke
  description: Smoke him:// routes (generated from manifest).
defaults:
  approved: true
  dry_run: true
do:
- him://local/mouse/query/status
- him://local/mouse/command/move: {}
- him://local/mouse/command/click: {}
```

```markdown markpact:docs
## Unpack → `.markpact/`

```bash
cd urihim
export TELLMESH_ROOT=/home/tom/github/tellmesh
PACK=markpacts/urihim.markpact.md
urisys markpact run "$PACK" --as flow --approve --dry-run
urisys markpact run "$PACK" --as pack
urisys markpact run "$PACK" --as service --port 8789
urisys markpact run "$PACK" --as interface
urisys markpact run "$PACK" --as adapter
```

- Source manifest: `urihim/urihim/manifest.yaml`
- Requires: `pip install -e urihim` or `TELLMESH_ROOT` for python:// handlers
```
